%
% Symboles.
%

% Ensemble des réels.
\newcommand{\R}{\ensuremath{\mathbb{R}}}

% Ensemble L^2(R).
\newcommand{\LRd}{\ensuremath{\mathbf{L}^2(\R)}}

% Ensemble L^2(R^2).
\newcommand{\LRdd}{\ensuremath{\mathbf{L}^2(\R^2)}}

% Peigne de diracs. Symbol ``il existe'' renversé.
\newcommand{\peigneII}{\scalebox{2}{\makebox[1.2\width][l]{\raisebox{0.85\depth}{\begin{turn}{-90}\ensuremath{\exists}\end{turn}}}}}

% Peigne de diracs 2e version. 2 ``U carrés'' collés.
\newcommand{\peigne}{\scalebox{1.5}{\mbox{\ensuremath{\sqcup}}\hspace{-0.4ex}\mbox{\ensuremath{\sqcup}}}}

% Symbole de convolution.
\newcommand{\conv}{\ensuremath{\star}}

% Symbole de convolution circulaire : étoile dans un cercle.
\newcommand{\ostar}{\mbox{$\bigcirc$}\hspace{-1.7ex}\mbox{$\star$}\hspace{0.8ex}}

% O majuscule (complexité).
\newcommand{\comp}{\ensuremath{\mathcal{O}}}

% Fonction porte, sous la forme 1[-T,T].
\newcommand{\porteII}[1]{\ensuremath{1_{[-#1,#1]}}}

% Fonction porte, sous la forme ``grand pi'' T.
\newcommand{\porte}[1]{\scalebox{1.5}{\mbox{\ensuremath{\sqcap}}}\ensuremath{_#1}}

% TF de la fonction porte, sous la forme ``grand pi'' T.
\newcommand{\tfPorte}[1]{\scalebox{1.5}{\mbox{\ensuremath{\hat{\sqcap}}}}\ensuremath{_#1}}
%\newcommand{\tfPorte}[1]{\ensuremath{\hat{\sqcap}_#1}}

% Redéfinition de \Re (partie réelle).
\renewcommand{\Re}{\ensuremath{\mathcal{R}}}

% Redéfinition de \Im (partie imaginaire).
\renewcommand{\Im}{\ensuremath{\mathcal{I}}}


%
% Transformée en ondelettes.
%

% Psi indice b,a.
\newcommand{\psiba}{\ensuremath{\psi_{b,a}}}

% Psi indice b,a (2d).
\newcommand{\psibaII}{\ensuremath{\psi_{\gb,\theta,a}}}

% Psi^ indice b,a.
\newcommand{\hatpsiba}{\ensuremath{\hat{\psi}_{b,a}}}

% Psi^ indice b,a (2d).
\newcommand{\hatpsibaII}{\ensuremath{\hat{\psi}_{\gb,\theta,a}}}

% t-b/a
\newcommand{\tMbSa}{\ensuremath{\frac{t-b}{a}}}

% x-b/a (2d)
\newcommand{\xMbSa}{\ensuremath{\frac{\gx-\gb}{a}}}

% r(x-b)/a (2d)
\newcommand{\rxMbSa}{\ensuremath{\frac{r_{-\theta}(\gx-\gb)}{a}}}

% To d'une fct.
\newcommand{\TO}[1]{\ensuremath{T_{#1}}}

% Produit scalaire.
\newcommand{\sca}[2]{\ensuremath{\langle#1,#2\rangle}}

% \psi en mode gras
\newcommand{\bpsi}{\boldsymbol{\psi}}

% T psi (en gras).
\newcommand{\Tpsi}{{\mathbf T}_{\bpsi}}

% T psi (pas en gras).
\newcommand{\tpsi}{T_{\psi}}

% T psi 1.
\newcommand{\Tpsiu}{T_{\psi_1}}

% T psi 2.
\newcommand{\Tpsid}{T_{\psi_2}}

% 
\newcommand{\npsi}{n_{\bpsi}}

% 
\newcommand{\Mpsi}{\ron{M}_{\bpsi}}

% 
\newcommand{\M}{\ensuremath{\ron{M}}}

% 
\newcommand{\A}{\ensuremath{\ron{A}}}

% 
\newcommand{\K}{\ensuremath{\ron{K}}}

% 
\newcommand{\Kp}{\ensuremath{\ron{K}'}}

% 
\newcommand{\Apsi}{\ron{A}_{\bpsi}}

% 
\newcommand{\sigw}{\sigma_W}

% Z(q,a)
\newcommand{\Zqa}{\ensuremath{\ron{Z}(q,a)}}

% M(p,a)
\newcommand{\Mpa}{\ensuremath{M(p,a)}}

% M(p,a')
\newcommand{\Mpap}{\ensuremath{M(p,a')}}

% L(a)
\newcommand{\La}[1][a]{\ensuremath{\ron{L}({#1})}}

% Lx0
\newcommand{\Lxz}{\ron{L}_{\gx_0}}

% Lx0(a)
\newcommand{\Lxza}{\ron{L}_{\gx_0}(a)}

% Lx1
\newcommand{\Lxu}{\ron{L}_{\gx_1}}

% Lx1(a)
\newcommand{\Lxua}{\ron{L}_{\gx_1}(a)}

% Lb(a)
\newcommand{\Lba}{\ron{L}_{\gb}(a)}

% Pa(M)
\newcommand{\PaM}{\ensuremath{P_a(\M)}}

% Pa(A)
\newcommand{\PaA}{\ensuremath{P_a(\A)}}

% Pa(M,A)
\newcommand{\PaMA}{\ensuremath{P_a(\M,\A)}}

% V_j
\newcommand{\Vj}{\gra{V}_j}

% W_j
\newcommand{\Wj}{\gra{W}_j}

% V_m1
\newcommand{\VV}{\gra{V}_{j-1}}

% P^log_j
\newcommand{\Plog}[1]{\ensuremath{P_{#1}^{(\log)}}}

% ^P^log_j
\newcommand{\hPlog}[1]{\ensuremath{\hat{P}_{#1}^{(\log)}}}

%
% Divers.
%

% Espace negatif a mettre apres un \right)
\newcommand{\negEspace}{\ensuremath{\!}}

%
\newcommand{\hmin}{\ensuremath{h_{\text{min}}}}

%
\newcommand{\hmax}{\ensuremath{h_{\text{max}}}}

% 1024 x 1024
\newcommand{\mxm}{\ensuremath{1024 \times 1024}}

% 1 / qque chose.
\newcommand{\uS}[1]{\ensuremath{\frac{1}{#1}}} 

% 1 / sqrt (qque chose).
\newcommand{\uSsq}[1]{\ensuremath{\frac{1}{\sqrt{#1}}}} 

% 1 / 2 pi.
\newcommand{\uSdpi}{\ensuremath{\frac{1}{2\pi}}} 

% 2 pi / qque chose.
\newcommand{\dpiS}[1]{\ensuremath{\frac{2\pi}{#1}}} 

% pi / qque chose.
\newcommand{\piS}[1]{\ensuremath{\frac{\pi}{#1}}} 

% qque chose /2.
\newcommand{\Sd}[1]{\ensuremath{\frac{#1}{2}}} 

% Intégrale simple de moins l'infini à plus l'infini.
\newcommand{\sint}{\ensuremath{\int_{-\infty}^{+\infty}}}

% Intégrale double de moins l'infini à plus l'infini.
\newcommand{\siint}{\ensuremath{\iint_{-\infty}^{+\infty}}}

% Un symbole entouré d'un cercle.
\newcommand{\circled}[1]{\scalebox{1.5}{\mbox{$\bigcirc$}}\hspace{-2.3ex}\mbox{#1}\hspace{0.9ex}}

% Un cercle venant de xfig pour les figures du chapitre numérique.
\newcommand{\cercle}{\mbox{\includegraphics{./figures_annexes/cercle.eps}}}

% Un cercle venant de xfig pour les figures du chapitre numérique.
\newcommand{\rond}{\mbox{\includegraphics{./figures_annexes/rond.eps}}}

% Redéfinition du signe '-'. Taille (et espaces) diminuée.
\newcommand{\minus}{\raisebox{0.3ex}{\ensuremath{{\scriptstyle -}}}}

% Redéfinition du signe '-'. Taille (et espaces) diminuée.
\newcommand{\plus}{\raisebox{0.3ex}{\ensuremath{{\scriptstyle +}}}}

%\def \dint {\D \int \! \! \D \int_{- \infty}^{+\infty}}
%\def \tint {\D \int \! \! \D \int \! \! \D \int_{- \infty}^{+\infty}}

% Dérivée successives...
\newcommand{\deriv}[2]{\ensuremath{\frac{d#1}{d{#2}}}}
\newcommand{\derivn}[3]{\ensuremath{\frac{d^{#1}#2}{d{#3}^{#1}}}}

% typo grasse en mode math.
\newcommand{\gra}[1]{{\mathbf{#1}}}

% x en gras.
\newcommand{\gx}{\gra{x}}

% y en gras.
\newcommand{\gy}{\gra{y}}

% r en gras.
\newcommand{\gr}{\gra{r}}

% s en gras.
\newcommand{\gs}{\gra{s}}

% b en gras.
\newcommand{\gb}{\gra{b}}

% u en gras.
\newcommand{\gu}{\gra{u}}

% l en gras.
\newcommand{\gl}{\gra{l}}

% k en gras.
\newcommand{\gk}{\gra{k}}

% \Delta en gras.
\newcommand{\gD}{\boldsymbol{\Delta}}
%
% Commandes de typographie générale et de raccourcis.
%

% Sigle ``transformée de Fourier''
\newcommand{\TeFlong}{transformée de Fourier\xspace}

% Sigle ``Transformée de Fourier''
\newcommand{\TeFLong}{Transformée de Fourier\xspace}

% Sigle ``transformation de Fourier''
\newcommand{\ToFlong}{transformation de Fourier\xspace}

% Sigle ``Transformation de Fourier''
\newcommand{\ToFLong}{Transformation de Fourier\xspace}

% Sigle ``TF''
\newcommand{\TFcourt}{{\scshape tf}\xspace}

% Sigle associé a la transformée de Fourier.
\newcommand{\TF}{\TFcourt}

% Sigle associé a la transformée de Fourier discrète.
\newcommand{\TFD}{{\scshape tfd}\xspace}

% Sigle ``FFT''
\newcommand{\FFT}{{\scshape fft}\xspace}

% Sigle ``FFTW''
\newcommand{\FFTW}{{\scshape fftw}\xspace}

% Sigle ``MMTO''
\newcommand{\MMTO}{{\scshape mmto}\xspace}

% Sigle ``MMMTO''
\newcommand{\MMMTO}{{\scshape mmmto}\xspace}

% Sigle ``TO''
\newcommand{\To}{{\scshape to}\xspace}

% Sigle ``1D''
\newcommand{\UD}{1D\xspace}

% Sigle ``2D''
\newcommand{\DD}{2D\xspace}

% Sigle ``3D''
\newcommand{\TD}{3D\xspace}

% Sigle ``WT''
\newcommand{\WT}{WT\xspace}



% Sigle ``FFT'' + taille entre parenthèse.
\newcommand{\FFTN}[1]{\ensuremath{\text{\scshape fft}(#1)}}

% Commande pour l'affichage des labels des sous figures (i.e. (a), (b), ...)
\newcommand{\subFig}[1]{{\upshape (#1)}}

% Commande pour l'affichage des symboles des figures (i.e. (\bullet), (\square), ...)
\newcommand{\figSymbol}[1]{{\upshape (#1)}}

% Commande pour (Éq. (2.61)) par exemple
\newcommand{\equa}[1]{(Éq.~(\ref{#1}))}


% Typo de terme que l'on définit pour la 1ère fois.
\newcommand{\defT}[1]{{\em #1}}


% Typo d'expressions en anglais.
\newcommand{\anglais}[1]{{\it #1}}

% Solution provisoire pour interdire des sauts de ligne au milieu d'une chaîne
% de caractères.
\newcommand{\nonewline}[1]{\mbox{#1}}

%
\newcommand{\eqRef}[1]{Éq.~(\ref{#1})}

%
\newcommand{\sigle}[1]{{\scshape #1}}

%
\newcommand{\scaleFigA}[1]{\scalebox{0.55}{#1}}

%
\newcommand{\scaleFigB}[1]{\scalebox{0.75}{#1}}

%
\newcommand{\scaleFigC}[1]{\scalebox{0.45}{#1}}

% typo ronde en mode math.
\newcommand{\ron}[1]{{\mathcal #1}}

% ``W-cascade''
\newcommand{\wCasc}{cascade $\ron{W}$}

% ``W-cascades''
\newcommand{\wCascs}{cascades $\ron{W}$}


\newcommand{\WTMM}{WTMM}
\newcommand{\WTMMM}{WTMMM}
%\newcommand{\WTMMM2}{WT3M}


\newcommand{\mycad}{\textit{c.-\`a-d.}}
\newcommand{\NS}{NS}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


% Environement remark.
%\newenvironment{remark}{\\*[1cm]\noindent\textsf{Remarque}\\*[.6cm]}{}


% Footnotes.
%\renewcommand{\thefootnote}{\fnsymbol{footnote}}
%\newcommand{\myfootnote}[1]{\stepcounter{footnote}\footnote{#1}\addtocounter{footnote}{-1}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Symboles et traits pour les figures.

% Symbole rond noir
\newcommand{\rondNoir}{\ensuremath{\bullet}}
\newcommand{\blackCircle}{\ensuremath{\bullet}}

% Symbole carre noir
\newcommand{\carreNoir}{\protect\scalebox{0.6}{\ensuremath{\blacksquare}}}
\newcommand{\blackSquare}{\protect\scalebox{0.6}{\ensuremath{\blacksquare}}}

% Symbole triangle noir
\newcommand{\triangleNoir}{\protect\scalebox{0.9}{\ensuremath{\blacktriangle}}}

% Symbole rond blanc
\newcommand{\rondBlanc}{\ensuremath{\circ}}
\newcommand{\whiteCircle}{\ensuremath{\circ}}

% Symbole carre blanc
\newcommand{\carreBlanc}{\protect\scalebox{0.6}{\ensuremath{\square}}}

% Symbole triangle blanc
\newcommand{\triangleBlanc}{\protect\scalebox{0.6}{\ensuremath{\triangle}}}

% Symbole croix : 'x'
\newcommand{\croix}{\ensuremath{\times}}

% Symbole etoile (style asterisque) : '*'
\newcommand{\etoile}{\ensuremath{\ast}}

% Symbole pentagone blanc
\newcommand{\pentagone}{\mbox{\epsfig{file=./pentagone.eps}\par}}

% Trait plein
\newcommand{\traitPlein}{{\bf ---}}

% Trait plein fort
\newcommand{\traitPleinFort}{\protect\raisebox{0.55ex}{\ensuremath{\linethickness{1.2pt}\line(50,0){15}}}}

% Trait en pointillés
\newcommand{\traitPointilles}{\protect\raisebox{0.5ex}{{\tiny .....}}}

% Trait en pointillés forts
\newcommand{\traitPointillesForts}{\protect\raisebox{0.5ex}{....}}

% Trait interrompu
\newcommand{\traitInterrompu}{\mbox{-~-~-}}

% Trait plein fort + rond noir
\newcommand{\traitFortRondNoir}{%
  \protect%
  \ensuremath{\;\!\bullet\;\bullet}%
  \hspace*{-3.5ex}%
  \raisebox{0.57ex}{%
    \ensuremath{%
      \linethickness{1.2pt}\line(50,0){18}}}}


\newcommand{\clearemptydoublepage}{\newpage{\pagestyle{empty}\cleardoublepage}}
